const express=require('express')
const router=express.Router();




router.post("/bookroom", async(req,res)=>{
    const {
        room
        ,fromdate
        ,todate
        ,totalDays
         ,totalAmount,user,token
        
    }=req.body
    try {
        
    } catch (error) {
        
    }
})